<Page
    x:Class="HelloWorld.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HelloWorld"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel HorizontalAlignment="Center">
        <!-- TODO: Provide Selection of Harry Potter's sentence -->
        <StackPanel x:Name="inputTextStackPanel" Orientation="Horizontal">
            <TextBox x:Name="textBoxInput" Height="200" Width="300" Margin="10"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 Header="Input your note" Text="October arrived, spreading a damp chill over the grounds and into the castle. Madam Pomfrey, the nurse, was kept busy by a sudden spate of colds among the staff and students. Her Pepperup potion worked instantly, though it left the drinker smoking at the ears for several hours afterward. Ginny Weasley, who had been looking pale, was bullied into taking some by Percy. The steam pouring from under her vivid hair gave the impression that her whole head was on fire."
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 SelectionChanged="TextBoxInput_SelectionChanged" />
            <Button x:Name="inputButton" Content="Read Content" Margin="10" Click="TextBoxInputButton_Click"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBox x:Name="selectedTextBox" Height="200" Width="300" Margin="10"
                     Header="Your selected text"
                     TextWrapping="Wrap" AcceptsReturn="True" IsReadOnly="True"/>
            <Button x:Name="selectedButton" Content="Read Content" Margin="10" Click="TextBoxSelectedButton_Click"/>
        </StackPanel>
        <TextBlock x:Name="label1" HorizontalAlignment="Center"/>
        <TextBlock x:Name="label2" HorizontalAlignment="Center"/>
    </StackPanel>
</Page>
